{% extends 'base.html.twig' %}

{% block title %}Carte{% endblock %}

{% block body %}
    <h1>Carte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carte.id }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ carte.categorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestion_carte_index') }}">back to list</a>

    <a href="{{ path('app_gestion_carte_edit', {'id': carte.id}) }}">edit</a>

    {{ include('gestion_carte/_delete_form.html.twig') }}
{% endblock %}
