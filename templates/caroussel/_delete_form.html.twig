<form method="post" action="{{ path('app_caroussel_delete', {'id': caroussel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ caroussel.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
