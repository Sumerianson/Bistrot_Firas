{% extends 'base.html.twig' %}

{% block title %}Editer la Formule
{% endblock %}

{% block body %}
	<div class="page">
		<h1>Editer la Formule</h1>
		<div class="validation">
			{{ include('formule/_form.html.twig', {'button_label': 'mettre à jour'}) }}

			<a href="{{ path('app_formule_index') }}" class="btn btn-primary">retour à la liste</a>

			{{ include('formule/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}
